type Query {
  hello(name: String): String!
}

type Mutation {
  register(
    firstName: String!
    lastName: String!
    email: String!
    password: String!
  ): Boolean
}

type User {
  firstName: String!
  lastName: String!
  password: String!
  email: String!
}
