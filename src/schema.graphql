type Query {
  hello(name: String): String!
}

type Mutation {
  register(
    userName: String!
    email: String!
    password: String!
  ): Boolean
}

type User {
  userName: String!
  password: String!
  email: String!
}
